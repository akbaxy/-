server:
  port: 8123
  servlet:
    context-path: /api
spring:
  application:
    name: yu-picture-backend
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://111.228.48.238:3306/yu_picture
    username: root
    password: wang14180857
# mybatis配置
mybatis-plus:
  configuration:
    # 开启驼峰命名
    map-underscore-to-camel-case: false
    # 开启日志（仅在开发环境）
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # 开启逻辑删除
  global-config:
    db-config:
      logic-delete-field: isDelete # 逻辑删除字段名
      logic-delete-value: 1 # 逻辑已删除值
      logic-not-delete-value: 0 # 逻辑未删除值

# 接口文档配置
knife4j:
  enable: true
  openapi:
    title: 接口文档
    description: 云图库接口文档
    email: 2862118982@qq.com
    concat: 小马利亚
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.yupi.yupicturebackend.controller